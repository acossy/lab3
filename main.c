#include <stdio.h>

int add(int x, int y)
{
    return x+y;
}

int diff(int x, int y)
{
    return x-y;
}

int main(int argc, char cons *argv[])

{
   printf("bonjour");
   printf("\n ADD=%d", add(3,2));
   printf("\n DIFF=%d", diff(3,2));
  
    return 0;
}
